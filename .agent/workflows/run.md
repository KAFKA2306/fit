---
description: リターゲット処理の実行
---

以下の手順で、衣装のリターゲット処理を実行します。

1. `task run` を実行します。
// turbo
2. 内部で `uv run python src/main.py --config config/standard.yaml` のようなコマンドが走り、Blenderがバックグラウンドで起動します。
3. 処理の進捗はターミナルに表示されます。
4. 完了後、指定された出力パスにリターゲット済みのFBXが保存されます。

エラーが発生した場合は、ログを確認し、設定ファイルやパスが正しいか確認してください。
