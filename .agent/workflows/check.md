---
description: 構文チェックとフォーマットの実行
---

以下の手順で、コードの品質チェックを行います。

1. `task check` を実行して、Ruffによる静的解析とフォーマットチェックを行います。
// turbo
2. `uv run ruff check src` を実行して、エラーがないことを確認します。
3. `uv run ruff format src` を実行して、スタイルを整えます。

すべてのチェックがパスすることを確認してください。
